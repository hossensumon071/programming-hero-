/* 
    TASKS: 
    1. some middle console log will be logged after 3.5 seconds

    2. take a number from users, using and add 250 to that number, and then display that using alert

    3. what is cookies, write in english at least 5-7 lines

    4. write at least differences beteween local storage and session storage

    5. differences beteween cookies, localstorage and session storage 

    6. what is event loop in javascript
    7. how asynchronous and synchronous code work in js
    8. Tell me four things that are asynchronous in js
*/

// Practice Task for local storage (level-1)
        // 1. clear your local storage
        // 2. Set a local storage ClipboardItem, Key:name, Value:SHakib Khan
        // 3.Set a local storage ClipboardItem, Key:age, value:40
        // 4. Get the local storage item you have set
        // 5. Remove the local storage key name & key age
        // 6. Set object as local storage ClipboardItem, the object is {firstName: 'Abrham , LastNAme: 'Linkon}

// Practice Task for local storage (level-02)
        // 1. Html file will have a p tag and button
        // 2. The p tag will show the count which is initially is 0
        // 3. Every TimeRanges, after clicking the button, the number will be increased by 1 & the result will be shown in the p tag
        // 4. with every click on the button the result will be stored in the local storage also
        // 5. Now the result stored in local storage. so if you reload the page or close tha tab & open again, the result the count is not 0. Rather it show the value from local storage without resel